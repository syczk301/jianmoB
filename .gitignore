# This repository publishes only root-level Python source and Git configuration.
# Inputs, fitted parameters, results, notebooks and caches stay local.
/*
!/*.py
!/.gitignore
!/.gitattributes

# Windows download metadata is never source code.
*:Zone.Identifier
